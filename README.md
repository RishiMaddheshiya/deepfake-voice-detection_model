# deepfake-voice-detection_model
A complete end-to-end machine learning pipeline for detecting deepfake (AI-generated) audio. Includes feature extraction (MFCC, spectral features), ensemble modeling (RF + GB + LR), evaluation metrics (EER, F1), and a Streamlit web app for real-time prediction.
